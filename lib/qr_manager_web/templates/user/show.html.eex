<h1>Show User</h1>

<ul>

  <li>
    <strong>Login:</strong>
    <%= "#{@user.first_name} #{@user.last_name}" %> 
  </li>

</ul>

<span><%= link "Show URLs", to: Routes.user_url_path(@conn, :index, @user) %></span>
<span><%= link "Edit", to: Routes.user_path(@conn, :edit, @user) %></span>
<span><%= link "Back", to: Routes.user_path(@conn, :index) %></span>
